import {} from "../types"

let initialState = {
    time: 1500
}

const clockReducer = (state=initialState, action) =>{
    switch(action.type){
        default:
            return state
    }
}


export default clockReducer